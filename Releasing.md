# Releasing miniops

This document describes the release flow for `miniops`.

## Release checklist

1. Update the package version in `pyproject.toml`.
2. Run tests locally:

   ```bash
   pytest